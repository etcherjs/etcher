{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^build"]
        },
        "test": {
            "inputs": ["src/**/*.ts", "test/**/*.spec.ts"]
        }
    }
}
